name: rce
on:
  workflow_dispatch:
  push: { paths: ['harness/rce.ps1', '.github/workflows/rce.yml'] }
jobs:
  winps51:
    name: Windows PowerShell 5.1
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: powershell
        run: .\harness\rce.ps1
