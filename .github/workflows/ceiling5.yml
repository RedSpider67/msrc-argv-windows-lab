name: ceiling5
on:
  workflow_dispatch:
  push: { paths: ['harness/ceiling5.ps1', '.github/workflows/ceiling5.yml'] }
jobs:
  winps51:
    name: Windows PowerShell 5.1
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - shell: powershell
        run: .\harness\ceiling5.ps1
