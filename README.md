# msrc-argv-windows-lab

Authorized security-research harness. It runs synthetic marker payloads on a Windows runner to settle Windows-only argument-parsing questions for a coordinated vulnerability report. No external service is contacted and nothing destructive runs.
